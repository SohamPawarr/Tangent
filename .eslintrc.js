module.exports = {
  plugins: ['json'], // Add the 'json' plugin
  extends: ['eslint:recommended', 'plugin:json/recommended'], // Extend with JSON plugin recommendations
  // Other configurations (optional)
};
